name: Time Trigger Task
on:
  workflow_dispatch: # 允许手动点击按钮触发，方便测试
jobs:
  run_tasks:
    runs-on: ubuntu-latest
    permissions:
      contents: write  # 需要写权限来提交 executed 状态的更改
    steps:
      - name: Checkout code
        uses: actions/checkout@v6

      - name: Install uv
        uses: astral-sh/setup-uv@v7

      - name: Set up Python
        run: uv python install

      - name: Run Task Script
        env:
          # 关键：将 GitHub Secret 注入为环境变量
          DEVICE_KEYS: ${{ secrets.DEVICE_KEYS }}
        run: uv run __init__.py

      # 6. 提交更改 (如果 __init__.py 修改了 json 文件的 executed 状态)
      - name: Commit and Push changes
        uses: stefanzweifel/git-auto-commit-action@v7
        with:
          commit_message: "🤖 Auto: Update task status [skip ci]"
          file_pattern: "configs/*.json" # 只提交 configs 目录下的 json 变化
